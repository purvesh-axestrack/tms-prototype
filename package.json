{
  "name": "tms-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install --include=dev && cd ../server && npm install",
    "build": "cd frontend && VITE_API_URL=/api npm run build",
    "start": "cd server && npx knex migrate:latest && npx knex seed:run && node index.js",
    "db:setup": "cd server && npx knex migrate:latest && npx knex seed:run"
  },
  "devDependencies": {
    "shadcn": "^3.8.5"
  }
}
